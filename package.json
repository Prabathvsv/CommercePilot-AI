{
  "name": "commercepilot-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start:web --workspace=apps/web",
    "dev": "npm run dev --workspaces --if-present",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspaces --if-present",
    "build:vercel": "npm run build --workspace=packages/shared && npx prisma generate --schema=packages/database/prisma/schema.prisma && npm run build --workspace=packages/database && npm run build --workspace=packages/ai && npm run build --workspace=packages/agents && npm run build --workspace=apps/api && npx esbuild apps/api/src/server.ts --bundle --platform=node --format=cjs --target=node20 --outfile=apps/api/dist/server.bundle.cjs --external:@prisma/client && npm run build --workspace=apps/web",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "@types/supertest": "^6.0.2",
    "@vercel/node": "^12.0.0",
    "concurrently": "^8.2.0",
    "supertest": "^6.3.4",
    "typescript": "^5.4.0",
    "vitest": "^1.2.2"
  }
}
